#ifndef OPENCLX_DEVICE_LOCAL_MEMORY
#define OPENCLX_DEVICE_LOCAL_MEMORY

#include <openclx/forward>

namespace clx {

    enum class device_local_memory: device_local_memory_type {
        local=CL_LOCAL,
        global=CL_GLOBAL,
    };

}

#endif // vim:filetype=cpp

